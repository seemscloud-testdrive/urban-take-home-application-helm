apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "take-home.fullname" . }}-test-connection"
  labels:
    {{- include "take-home.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "take-home.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
